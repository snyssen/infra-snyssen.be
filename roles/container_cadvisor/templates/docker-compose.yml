version: "2"
services:
  cadvisor:
    image: gcr.io/cadvisor/cadvisor:0.47.2
    container_name: cadvisor
    ports:
      - 9200:8080
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:rw
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
    mem_limit: 364m

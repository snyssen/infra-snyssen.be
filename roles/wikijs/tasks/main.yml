- name: Creating folders
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
  with_items:
    - "{{ docker_mounts_directory }}/wiki/postgresql"
    - "/mnt/storage/wiki/backups"

- ansible.builtin.include_role:
    name: container-deploy
  vars:
    container_name: "wikijs"
    docker_compose_start: yes

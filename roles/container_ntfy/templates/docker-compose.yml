services:
  ntfy:
    image: binwiederhier/ntfy:v2.13.0@sha256:7b6d19000f112300f33a56c77e22735452d9ed2f3faa4fbb8e3d0a9a639fa5e6
    container_name: ntfy
    restart: unless-stopped
    command:
      - serve
    environment:
      TZ: "{{ iana_timezone }}"
      NTFY_BASE_URL: "https://ntfy.{{ ansible_fqdn }}"
      NTFY_CACHE_FILE: /var/cache/ntfy/cache.db
      NTFY_CACHE_DURATION: 48h
      NTFY_BEHIND_PROXY: true
      NTFY_ATTACHMENT_CACHE_DIR: /var/cache/ntfy/attachments
      NTFY_AUTH_FILE: /ntfy/auth/auth.db
      NTFY_ENABLE_LOGIN: true
      NTFY_AUTH_DEFAULT_ACCESS: deny-all
      NTFY_METRICS_LISTEN_HTTP: ":9090"
    volumes:
      - "{{ docker_mounts_directory }}/ntfy/cache:/var/cache/ntfy"
      - "{{ docker_mounts_directory }}/ntfy/auth:/ntfy/auth"
    healthcheck:
      test: ["CMD-SHELL", "wget -q --tries=1 http://localhost:80/v1/health -O - | grep -Eo '\"healthy\"\\s*:\\s*true' || exit 1"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - web
    labels:
      - traefik.enable=true
      - traefik.docker.network=web
      - traefik.http.routers.ntfy.entryPoints=websecure
      - traefik.http.routers.ntfy.rule=Host(`ntfy.{{ ansible_fqdn }}`)
      - traefik.http.routers.ntfy.service=ntfy
      - traefik.http.services.ntfy.loadBalancer.server.port=80
networks:
  web:
    external: true
  monitoring:
    external: true

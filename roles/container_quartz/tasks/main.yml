- ansible.builtin.include_role:
    name: snyssen.compose_deploy
  vars:
    stack_name: quartz

- name: Create monitors
  lucasheld.uptime_kuma.monitor:
    name: "notes - {{ item.name }}"
    type: http
    url: "https://{{ item.name }}.notes.{{ main_domain }}"
  loop: "{{ quartz__vaults }}"

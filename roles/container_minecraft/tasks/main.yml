- name: Creating folders
  ansible.builtin.file:
    path: "/mnt/storage/backups/minecraft"
    state: directory
    owner: "{{ ansible_user_uid }}"

- ansible.builtin.include_role:
    name: container_deploy
  vars:
    container_name: minecraft

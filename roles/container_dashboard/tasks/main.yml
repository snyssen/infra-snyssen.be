- name: Creating folders
  ansible.builtin.file:
    path: "/mnt/storage/backups/heimdall"
    state: directory
    owner: "{{ ansible_user_uid }}"

- ansible.builtin.include_role:
    name: container_deploy
  vars:
    container_name: dashboard

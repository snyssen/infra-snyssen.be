DOCKER_DIRECTORY={{ docker_mounts_directory }}
HOSTNAME={{ ansible_fqdn }}
SMTP_SERVER={{ smtp.host }}
SMTP_PORT={{ smtp.port }}
SMTP_USER={{ smtp.user }}
SMTP_PASSWORD={{ smtp.pass }}
SMTP_DOMAIN={{ ansible_fqdn }}
SMTP_FROM={{ smtp.from }}
SMTP_TO={{ smtp.to }}
SSH_PORT={{ gitlab.ssh_port }}
ADMIN_PASS={{ gitlab.admin_pass }}

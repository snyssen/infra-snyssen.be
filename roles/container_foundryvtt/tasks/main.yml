- name: Creating folders
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
  loop:
    - /mnt/storage/foundryvtt

- ansible.builtin.include_role:
    name: snyssen.compose_deploy
  vars:
    container_name: foundryvtt

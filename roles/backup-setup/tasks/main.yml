- ansible.builtin.include_role:
    name: container-minio

# minio treats every folder found inside its data root (here set to /mnt/storage/data) as a bucket
# So creating a new bucket is as simple as creating a folder !
- name: Creating backup bucket
  become: yes
  ansible.builtin.file:
    path: "/mnt/storage/data/{{ minio.backup_bucket_name }}"
    state: directory

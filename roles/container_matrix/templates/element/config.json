{
    "default_server_config": {
        "m.homeserver": {
            "base_url": "https://matrix.{{ main_domain }}",
            "server_name": "matrix.{{ main_domain }}"
        }
    },
    "disable_custom_urls": true,
    "disable_guests": true,
    "brand": "matrix.{{ main_domain }}",
    "default_theme": "light",
    "roomDirectory": {
        "servers": [
            "matrix.{{ main_domain }}"
        ]
    }
}

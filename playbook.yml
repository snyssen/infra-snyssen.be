- hosts: all
  vars_files:
    - vars/vault.yml
  tasks:

    - ansible.builtin.include_tasks: tasks/softwares.yml
    - ansible.builtin.include_tasks: tasks/disks.yml
    - ansible.builtin.include_tasks: tasks/shell.yml

    - name: Rebooting server to apply changes
      become: yes
      ansible.builtin.reboot:

    - ansible.builtin.include_tasks: tasks/containers.yml

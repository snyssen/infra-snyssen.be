- hosts: all
  roles:
    - packages_upgrade
    - setup_common
    - setup_storage

- hosts: apps
  roles:
    - role: setup_snapraid
      vars:
        should_skip: "{{ skip_snapraid | default(false) }}"
      when: not should_skip
    - stacks_deploy

- hosts: backup
  roles:
    - container_rest
